import styled from 'styled-components'

export const AppWrapper = styled.section`
    width: inherit;
    min-height: 100vh;
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
`